<div>
    <button id="close-button">X</button>
    <div>{{user.email}}</div>
    <div>{{user.phone}}</div>
    <div>{{user.userId}}</div>
    <div>Most recent day user did at least 18 minutes: {{user.status.last18MinutesDate}}</div>
    <div>Most recent day user played at least 3 games: {{user.status.last3PlaysDate}}</div>
    <div>Total number of minutes {{dateRange}}, divided by 18: {{user.sessEstimate}}</div>
    <div>Total number of games played {{dateRange}}: {{user.status.recentLumosityCount}}</div>
    <div id=""></div>
    <div id="pay-info"></div>
    <div id="pay-errors"></div>
</div>